# prepack-webpack-plugin

```bash
$ (cd .. && npm install && npm run build)
$ npm install
$ cat src/index.js
$ cat src/chunk.js
$ npm run build
$ cat dist/index.bundle.js
$ cat dist/index.chunk.js

```
